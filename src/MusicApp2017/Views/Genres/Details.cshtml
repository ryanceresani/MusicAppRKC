@model MusicApp2017.Models.Genre

@{
    ViewData["Title"] = "Details";
}

<h3>Genre Details</h3>

<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd>
            @Model.Name
        </dd>
        <dt>
            Albums
        </dt>
        @foreach (var a in ViewBag.Albums)
        {
            <dd>
                <a asp-Controller="Albums" asp-Action="Details" asp-route-id="@a.AlbumID">@a.Title</a>
            </dd>
        }
    </dl>
</div>
<div>
    <a class="btn btn-warning" asp-action="Edit" asp-route-id="@Model.GenreID">Edit</a>
    <a class="btn btn-primary" asp-action="Index">Back to List</a>
</div>
